largura = float(input('Qual a largura da parede: '))
altura = float(input('Qual a altura da parede: '))
area = largura * altura
tinta = area / 2
print('Área total da parede: {} metros quadrados. Serão necessários {} litros de tinta'.format(area, tinta))
